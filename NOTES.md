# NOTES.md — harness-improvement journal

- 2026-08-16 18:49 — Run start (first invocation: spec + scaffold + Phase 1). Branch feature/on-the-road-20260816-1849-v0.1. Remote origin present (github.com/smallTechOrg/on-the-road).
- 2026-08-16 ~20:10 — Phase 1 complete. All 5 slices (1A-1E) VERIFIED first pass; no BLOCKED loops. Gate: 78 tests + Playwright smoke green. Friction log: (1) slice 1A had to guard router imports inside create_app since 1C built concurrently — worked as designed; (2) slice 1E needed 3 small glue fixes (main.py wiring, /api/me, adapter factory) — expected integration cost of parallel slices; (3) phase-gate auditor misread `git status` as being on main (we were on the feature branch) — harmless but note auditors should report branch name, not assume.
- 2026-08-16 ~21:30 — Phase 2 build started (prior orchestrator). Killed mid-flight; partial output left uncommitted: slices 2A (jam.js, chat.js edits), 2B (usage tables, rollup, api/usage.py), 2C (preview proxy) partially done; 2D (approvals/git) absent.
- 2026-08-16 ~22:45 — Phase 2 resumed from partial tree. Audited all partial files (all import/parse cleanly). Two generators fanned out: one finishing 2B (usage.js, error audit middleware, main.py wiring, integration tests, live test extension) + 2C tests; one building 2D from scratch (interactive approvals replacing auto-approve, echo_child !git/!permission triggers, api/approvals.py, git.js, manager plumbing, test_acp_client fix, integration tests). QA auditor found two blockers: (1) preview.js seen-set not reset on chat re-render causing chips to vanish on jam/debug toggle — fixed with _resetSeen() call; (2) hermes_acp.py timeout fallback auto-approved (spec requires reject-safe default for disconnected-phone scenario) — fixed to _reject_safe_option + updated test + git.js copy. Gate: 117 passed. Live gate: Hermes ACP runtime not installed in this environment (wrong `hermes` binary on PATH); adapter correctly detects setup-only authMethods; code is correct, env limitation.
- 2026-08-17 ~02:38 — Phase 3 complete. Three slices (3B terminal, 3C diff/files, 3D screenshot) built in parallel and verified. Slice 3A (push notifications) explicitly deferred by user. Gate: 146 tests green; Docker build + run + healthz + Echo session all verified inside container. Terminal uses a PTY WebSocket with <pre>-based output + ANSI-to-HTML; diff viewer has server-side git diff/file read/tree endpoints with mobile-friendly rendering; screenshot endpoint returns 501 gracefully when Playwright isn't installed (kept out of production image). Notifications bell remains a stub labelled "deferred". Empty src/ontheroad/push/ directory removed.
